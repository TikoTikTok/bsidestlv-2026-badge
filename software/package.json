{
  "name": "alice-in-ai-land",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Alice in AI Land - a boulder-pushing puzzle game served as static files, plus the tools that cut its art.",
  "scripts": {
    "gen": "node tools/gen-world.mjs && node tools/gen-cards.mjs && node tools/gen-bosses.mjs",
    "validate": "node tools/validate.mjs",
    "verify": "node tools/verify-stage.mjs && node tools/verify-chase.mjs",
    "export": "node tools/export-png.mjs",
    "build": "npm run gen && npm run validate && npm run export",
    "serve": "python3 -m http.server 8080 --directory site"
  }
}
